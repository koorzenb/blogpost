# About

Website for  posting 

First user to register will be admin. Subsequent users will be regular users.

# Development

In root folder, run:

```bash
py -m venv venv 
venv\Scripts\activate
@REM pip install -r requirements_3.13.txt
py main.py
```
